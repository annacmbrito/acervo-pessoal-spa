export interface Book {
    
}

export interface BookDTO {
    name: string
    description: string
    comment: string
    pages: number
    rating: number
    status: string
    author: string,
    language: string,
    publisher: string,
    category: string,
    subcategory: string,
}